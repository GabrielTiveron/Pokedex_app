<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabrieltiveron.ep_2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.Pokedex" />
        <activity android:name=".view.CadastroTreinador" />
        <activity android:name=".view.DetalhesPokemon" />
        <activity android:name=".view.ListaTreinadores" />
        <activity android:name=".view.AdapterListaTreinadores" />
        <activity android:name=".view.DetalhesTreinador"></activity>
    </application>

</manifest>